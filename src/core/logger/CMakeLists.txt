#Generated by VisualGDB project wizard.
#Note: VisualGDB will automatically update this file when you add new sources to the project.

cmake_minimum_required(VERSION 2.7)
project(logger)
set(LIBRARIES_FROM_REFERENCES "")
#set(EXECUTABLE_OUTPUT_PATH ${CMAKE_CURRENT_SOURCE_DIR}/../bin)

find_package(roscpp REQUIRED)
include_directories(${roscpp_INCLUDE_DIRS})

find_package(jukovsky_ros REQUIRED)
include_directories( ${jukovsky_ros_INCLUDE_DIRS})

add_executable(${PROJECT_NAME} logger.cpp logger.h)
target_link_libraries(${PROJECT_NAME} ${roscpp_LIBRARIES} "${LIBRARIES_FROM_REFERENCES}")
